name: my workflow #workflowname

on:                                 #events that trigger that trigers this workflow

    push:
        branches: [main, develop]

    jobs:                           #one or more jobs
        my-job:                     #job id or name
            runs-on: ubuntu-latest  #the type of machine to run on, runer os
            steps:                  #sequential steps
                - name: checkout code
                  uses: actions/checkout@v4 #reusable action
                - name: say hello
                  run: echo "Hello World"  #shell command
                    echo "Running tests..."
                    # add your test commands here
                - name: build
                  run: |
                    echo "Building..."
                    # add your build commands here
                - name: deploy
                  run: |
                    echo "Deploying..."